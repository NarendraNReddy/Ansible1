- name: variables
  hosts: localhost
  vars:
    Course: "Terraform"
    Trainer: 'Siva'
    Duration: "15hrs"

  become: no
  tasks:
  - name: Variable declaration
    vars:
      Course: "DevOps"    
    ansible.builtin.debug:
      msg: "Hi Override ,we are learning {{Course}} from {{Trainer}},duration is {{Duration}}"

  - name: Variable declaration
    ansible.builtin.debug:
      msg: "Hi Inheritance ,we are learning {{Course}} from {{Trainer}},duration is {{Duration}}"      